// Display help information

#include "help.h"

void display_help_info() {
    system("cls");
    system("more help.txt");

    // FILE *file = fopen("help.txt", "r");
    // if (file == NULL) {
    //     perror("Open help file failed");
    // }

    // char ch;
    // while ((ch = fgetc(file)) != EOF) {
    //     putchar(ch);
    // }

    // fclose(file);
}
